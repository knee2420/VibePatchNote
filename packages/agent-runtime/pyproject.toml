[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "agent-runtime"
version = "0.1.0"
description = "Pure Host-independent & Domain-agnostic Agent Run Lifecycle, Event-Sourcing and Human Approval Engine"
readme = "README.md"
requires-python = ">=3.10"
authors = [
    { name = "VibePatchNote Team" }
]
dependencies = [
    "pydantic>=2.7.0",
]
